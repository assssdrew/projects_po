# Prototypes

Фрагменты UI из предыдущей итерации. Это не компилируемый проект целиком — только `ContentView`.

Предпочтительная база для дальнейшей app-части: `ContentView+FlashcardTTS.swift` (карточка + озвучка).  
Список (`ContentView+List.swift`) полезен как экран обзора словаря.
