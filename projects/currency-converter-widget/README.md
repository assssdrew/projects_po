# Конвертер валют — Scriptable

Home Screen виджет на **Scriptable** (см. `notes/currency_converter_options.md`,
вариант 2): курсы валют на виду + интерактивная конвертация суммы по тапу.
Стек и структура — по образцу `../vocab-iphone-widget/`.

## ⚠️ Если виджет выглядит не так, как в описании ниже

Самая частая причина — **на телефоне не тот код**, а не баг в новой версии.
Виджет на Home Screen (в отличие от запуска ▶ Play внутри приложения)
**не обновляет свою логику при каждой перерисовке** — он использует то, что
было скачано при последнем ручном запуске. Если после моих правок ты просто
смотрел на виджет / тапал по нему на Home Screen, а не открывал сам
**скрипт в приложении Scriptable и не нажимал там ▶ Play** — на экране всё
ещё могла показываться логика от одной из промежуточных версий, а не
последняя. С v4.1 виджет умеет обновлять код сам, но только если кэш кода
старше 3 часов — поэтому после моих правок быстрее всего:

1. Открой приложение **Scriptable** (не виджет, а само приложение)
2. Найди скрипт `CurrencyConverter` в списке и нажми на него → ▶ **Play**
3. Должно открыться меню — в заголовке будет написано **«Конвертер валют · vX.X»**
4. Сверь номер версии с тем, что в этом README (сейчас `6.15`) — если меньше,
   значит обновление ещё не подтянулось (проверь сеть и попробуй ▶ Play ещё раз)
5. После этого либо подожди системной перерисовки виджета, либо удали и
   заново добавь его на Home Screen — так гарантированно перерисуется сразу

### v4.3: если и это не помогает — «ничего не меняется» на плитке

Раньше при **любой** непредвиденной ошибке внутри отрисовки виджета (а не
внутри меню) код просто молча завершался, не вызывая `Script.setWidget()` —
и WidgetKit в такой ситуации не показывает ошибку, а **оставляет на плитке
тот же самый рендер, что был до этого**, сколько угодно раз подряд. Со
стороны это выглядит ровно как «ничего не меняется», даже если реальный код
на телефоне давно новый — то есть версия в меню (`▶ Play`) может быть уже
`4.x`, а сама плитка на Home Screen выглядеть так же, как год назад.

С v4.3 это исправлено на обоих уровнях (bootstrap и основная логика): если
что-то ломается при отрисовке плитки, вместо тишины виджет покажет
**тёмно-красную аварийную плитку с текстом самой ошибки** и номером версии.
Если ты видишь такую плитку — это хороший знак: значит, код обновляется, и
дальше нужно просто прислать текст ошибки с неё, чтобы я поправил конкретный
баг. Если после обновления до `4.3`+ (см. шаги 1–5 выше) на плитке **по-прежнему
старый вид без единого визуального отличия и без красной плитки с ошибкой** —
значит, сам файл `CurrencyConverterCore.js` физически не долетает до
телефона (см. следующий раздел про версии и GitHub).

### v4.4: железный тест — перерисовывается ли плитка вообще

Рядом со значком обновления на **любом** стиле теперь есть подпись вида
`v4.4 · 14:32:07` — это не время обновления курсов, а момент, когда именно
эта плитка была физически нарисована кодом. Это самый прямой способ
проверить, доходят ли вообще изменения до Home Screen, независимо от того,
какой стиль выбран и насколько заметны визуальные отличия:

1. Открой **Scriptable** → `CurrencyConverter` → ▶ **Play**
2. В самом низу экрана появится маленькое превью виджета — посмотри на время
   рядом со значком обновления, например `v4.4 · 14:32:07`, и запомни его
3. Сразу же посмотри на настоящую плитку на Home Screen — сравни время в её
   такой же подписи с тем, что было в превью на шаге 2
4. Если время на Home Screen **сильно отстаёт** (не 14:32, а, например,
   вчерашнее или из позапрошлой правки) — значит WidgetKit действительно не
   перерисовывал плитку всё это время, и дело не в коде, а в системном
   расписании обновлений. Средство: удалить виджет с Home Screen и добавить
   заново (см. шаги выше); если и это не помогает — попробовать перезапустить
   телефон (крайняя, но иногда единственная мера против зависшего кэша
   WidgetKit) или полностью удалить и переустановить сам виджет-плитку (не
   приложение) через долгий тап → Remove Widget → добавить с нуля
5. Если время на Home Screen **совпадает или почти совпадает** с превью —
   значит перерисовка происходит исправно, а несовпадение с ожиданиями — уже
   вопрос конкретного визуального оформления стиля, а не обновления кода

### Разгадка: это не баг, это ограничение самого WidgetKit

Этой меткой времени удалось наконец получить конкретное доказательство:
плитка реально отрисовалась в правильном стиле, с правильными цифрами и
верным `CORE_VERSION` — код всё это время работал верно. Но метка показала
`00:25:19`, тогда как на телефоне было уже `12:25` — то есть **iOS не
перерисовывала плитку около 12 часов подряд**.

Это официально задокументированное поведение WidgetKit, а не что-то, что
можно починить в самом скрипте: Apple выделяет каждому виджету бюджет из
40–70 перерисовок в сутки, и система сама решает, когда его тратить —
обычно раз в 15–60 минут для часто просматриваемых виджетов, но интервал
может расти при слабом использовании, включённом режиме энергосбережения
или ограниченном фоновом обновлении. Ни один сторонний виджет (не только на
Scriptable) не может заставить iOS перерисовывать себя чаще или в реальном
времени — это сделано намеренно ради заряда батареи.

Виджет использует единственную официальную «подсказку» Scriptable для этого —
`refreshAfterDate`: просит систему не тянуть с перерисовкой курсов дольше
45 минут (для аварийной плитки с ошибкой — не дольше 5 минут, на случай если
сбой был временным). **Важно:** эта подсказка стояла в коде и раньше — то, что
на скриншоте разрыв был ~12 часов, произошло уже с ней. Это не гарантия
(система всё равно вправе подождать намного дольше при экономии заряда), а
именно подсказка «предпочтительного» момента — окончательное решение
целиком за iOS. Так что если разрыв всё ещё большой, дело не в самом коде, а
именно в настройках телефона ниже.

Что дополнительно можно проверить на телефоне, если разрыв всё ещё большой:

- **Настройки → Основные → Обновление контента в фоне** — должно быть
  включено глобально и отдельно для Scriptable
- **Настройки → Аккумулятор → Режим энергосбережения** — если включён,
  iOS резко ограничивает фоновые обновления виджетов
- Чем чаще открываешь сам виджет/приложение, тем больше бюджета на
  перерисовки система склонна выделять именно ему — это несколько дней
  накопления статистики использования, а не мгновенный эффект

### v5.0: оставлены только 2 стиля + точные референс-мокапы

Из четырёх стилей оставлены только **«Минимальный»** и **«Тикер»** — стили
«Карточки» и «Hero» полностью убраны из кода и из переключателя, чтобы не
плодить лишние варианты и сосредоточиться на двух, которые реально нужны.

Раньше единственным описанием стиля был текст в README — и то, что
получалось в реальном коде, могло не совпадать с тем, что изначально имелось
в виду словами. Теперь у каждого из двух стилей есть точный референс-мокап
(картинка ниже, в `mockups/minimal.png` и `mockups/ticker.png`) — код
реализует ровно то, что на нём: те же цвета, тот же шрифт, то же
расположение строк и подпись версии/времени в углу. Если в будущем внешний
вид на телефоне разойдётся с этими картинками — значит разошёлся именно
код, и это будет видно сразу по сравнению один-в-один, а не по словесному
описанию.

### v5.1: превью при ручном ▶ Play был непропорционально огромным — это не баг дизайна, а недостающий вызов `presentMedium()`

Если после ▶ Play в приложении вместо аккуратной маленькой плитки открывался
почти полноэкранный экран с текстом на весь экран, крестиком закрытия и
кнопкой «Поделиться» вверху — это не баг вёрстки виджета и не ограничение
задачи, а конкретная недостающая строчка кода. Без явного вызова
`widget.presentSmall()/presentMedium()/presentLarge()` Scriptable при ручном
запуске сама показывает содержимое виджета в собственном увеличенном
«быстром просмотре», который **не передаёт настоящий масштаб** плитки на
Home Screen — шрифты там нарисованы в тех же самых "поинтах", что и на
реальной маленькой плитке, но контейнер вокруг них растянут на весь экран,
поэтому всё выглядит гигантским и непропорциональным.

Исправлено: теперь после ▶ Play явно вызывается `previewWidget.presentMedium()`
— превью показывается в реальном масштабе виджета среднего размера, точно
как он будет выглядеть на Home Screen. Настоящая плитка на Home Screen (и
карточка в галерее выбора виджета при добавлении) всегда были в правильном
масштабе — непропорциональным был только этот один экран внутриприложенного
превью.

Заодно у стиля «Тикер» спарклайн теперь на каждой строке, а не только у
первой пары на large-виджете — ближе к референсу с несколькими рядами,
каждый с собственным мини-графиком.

### v5.2: строки с VND (и другими "длинными" валютами) выглядели вразнобой

Если среди пар есть валюта вроде VND, где обычная сумма превращается в
семи-восьмизначное число (например `147 USD → 3 826 947,39 VND`), эта
строка была заметно длиннее соседних (`147 USD → 12 241,3 RUB`). А так как
каждая строка ужимается по ширине независимо (`lineLimit(1)` +
`minimumScaleFactor`), длинная строка ужималась в шрифте намного сильнее —
и в итоге в одном и том же виджете строки получались визуально совсем
разного размера, что и выглядело "не так, как в примере" (там у всех строк
шрифт был одинаковый).

Исправлено: очень большие суммы (от миллиона) теперь сокращаются —
`3 826 947,39` становится `3,83 млн`, `1 200 000 000` — `1,2 млрд`. Строки
остаются примерно одной длины независимо от того, какая валюта в паре, и
шрифт больше не "скачет" между рядами.

Отдельно про баг с прозрачностью, который был в одной из версий: `ListWidget`
на Home Screen [по умолчанию должен быть непрозрачным](https://docs.scriptable.app/listwidget/#backgroundcolor),
а я явно ставил `Color.clear()`, из-за чего цветные строки сливались с обоями
и текст было не разобрать. Сейчас у каждого стиля свой явный непрозрачный фон.

**Ещё нюанс:** `raw.githubusercontent.com` и `jsdelivr` — это CDN со своим
кэшем, поэтому после `git push` в этот репозиторий новый код может доехать
туда не мгновенно, а с задержкой в несколько минут — это нормально. Если
номер версии в меню всё ещё старый сразу после пуша — подожди пару минут и
нажми ▶ Play ещё раз.

**Важнее всего:** даже когда номер версии в меню (запущенном через ▶ Play в
приложении) уже правильный — сама плитка виджета на Home Screen может
какое-то время выглядеть по-старому. Это отдельная система: iOS (WidgetKit)
перерисовывает виджет **по собственному расписанию**, а не сразу после
запуска скрипта вручную в приложении. Самый надёжный способ форсировать
немедленную перерисовку:

1. Долгий тап по виджету на Home Screen → **Remove Widget**
2. Долгий тап по пустому месту экрана → **+** → найди **Scriptable** →
   выбери размер → добавь
3. Долгий тап по новому виджету → **Edit Widget** → выбери скрипт
   `CurrencyConverter`
4. **Не забудь** сразу же в Edit Widget выставить **«When Interacting» =
   «Run Script»** — при удалении/добавлении виджета эта настройка
   сбрасывается на дефолт, и тап по виджету снова перестанет открывать меню,
   пока не проставишь её заново

### v5.3: почему результат расходился с "образцом" — и как это исправлено по-честному

Справедливый вопрос был задан прямо: раньше `mockups/minimal.png` и
`mockups/ticker.png` были **сгенерированы AI-моделью для изображений** —
то есть это была стилизованная художественная иллюстрация "по мотивам"
описания стиля, а не буквальная спецификация пикселей. У такой картинки
неизбежно будет **свой** шрифт, свои декоративные элементы (фон-планета,
градиенты) и своя расстановка отступов, которые генератор изображений
придумал сам — Scriptable физически не может нарисовать произвольный
шрифт из иллюстрации, у него только системные шрифты iOS (San Francisco /
SF Mono) и простая построчная вёрстка (`ListWidget`/`WidgetStack`). Сравнивать
такую картинку "один в один" с реальным рендером было некорректно с самого
начала — и то, что они разошлись по шрифту и декору, ожидаемо, а не
показатель того, что код не работает.

Что было реальным, воспроизводимым багом (а не разницей "мокап vs код»):

1. **Ряды выглядели несимметрично.** Стрелка `→` в «Минимальном» стиле и
   начало колонки «% изменения + график» в «Тикере» стояли на разной
   позиции от строки к строке, потому что до этой версии их сдвигала
   разная длина суммы слева (`147 USD` короче, чем `12 240,69 RUB`).
   **Исправлено:** первая колонка каждой строки (сумма+валюта в
   «Минимальном», пара и значение в «Тикере») теперь имеет фиксированную
   ширину (`stack.size = new Size(w, 0)`), поэтому стрелки и колонка
   изменения курса стоят строго друг под другом на любой строке — ряды
   выглядят как настоящая таблица, а не вразнобой.
2. **Графиков (спарклайнов) не было видно.** Спарклайн в «Тикере»
   технически уже был реализован (см. v5.1), но требует **минимум 2
   точки истории курса**, а точки писались не чаще раза в 6 часов — то
   есть после установки виджета первый график физически не мог
   появиться раньше, чем через ~6–12 часов. Внешне это выглядело как «у
   тебя вообще нет графиков», хотя код был на месте.
   **Исправлено:** порог снижен до 15 минут — открой меню → «Обновить
   курсы сейчас», подожди ~15 минут, нажми ещё раз (или тапни по значку
   обновления дважды с паузой) — и график должен появиться в тот же
   тестовый заход, а не через полдня.

Также заменены сами референсные картинки: `mockups/minimal.png` и
`mockups/ticker.png` — это больше не иллюстрации от генератора
изображений, а **прямой HTML/CSS-рендер**, куда переписаны те же самые
значения, что и в реальном коде (`#121316`/`#000000` фон, системный и
моноширинный шрифт, те же фиксированные ширины колонок, тот же текст).
Исходники лежат рядом (`mockups/minimal.html`, `mockups/ticker.html`) —
их можно открыть в браузере и свериться построчно с `CurrencyConverterCore.js`
буква в букву, а не полагаться на художественное сходство.

`CORE_VERSION` был `5.3`.

### v6.0: один стиль вместо переключателя, графики в нём же, без метки версии на плитке, +1 пара на виду

- **Убран переключатель стилей и «Тикер».** Остался только один стиль —
  «Минимальный». Меньше кода, меньше путаницы, меньше расхождений между
  тем, что реально настроено, и тем, что видно на плитке.
- **В «Минимальный» добавлены графики.** Раньше мини-график (спарклайн)
  был только у отдельного стиля «Тикер». Теперь он встроен прямо в
  единственный стиль — под каждой строкой рядом с `%` изменения курса за
  сутки (на `medium`/`large`; на `small` из-за ширины остаётся только
  текстовый `%`, без графика).
- **Убрана служебная метка `vX.X · ЧЧ:ММ:СС` с самой плитки.** Она была
  полезна на этапе диагностики "перерисовывается ли виджет вообще", но не
  нужна как постоянный элемент интерфейса. Номер версии для диагностики
  остался только в заголовке **меню** (тап по виджету), на плитке —
  никакого текста, кроме курсов.
- **Видно на одну пару больше.** Раньше `small` показывал 2 пары, `medium`
  — 3, `large` — 6. Теперь `small` — 3, `medium` — 4, `large` — 7. Чтобы
  4-я строка с графиком реально помещалась по высоте на `small`/`medium`
  (у них одинаковый вертикальный бюджет — `medium` только шире, а не
  выше), шрифт и отступы там стали чуть компактнее; `large` (существенно
  выше по высоте) остался с прежними, более крупными размерами.
- Добавлена 4-я пара по умолчанию: `USD-VND` — так эффект виден сразу после
  установки, а не только при ручной настройке через **«Настроить пары для
  виджета»**.

`CORE_VERSION` обновлён до `6.0`.

### v6.1: график был угловатой "молнией" из прямых линий, а не естественной кривой

Справедливое замечание: спарклайн соединял точки истории курса **прямыми
отрезками** (`path.addLines(points)`) — из-за этого график выглядел как
угловатая ломаная "молния", а не как естественная кривая, к которой все
привыкли по финансовым графикам (Apple Stocks и подобные всегда рисуют
сглаженную линию, а не соединяют точки напрямую).

Исправлено: график теперь строится сглаженной кривой — координаты точек те
же самые (реальная история курса), но между ними теперь кубические
Bezier-сегменты (`path.addCurve(...)`), посчитанные по формуле Catmull-Rom
(кривая проходит точно через все точки, но плавно, без острых углов на
стыках). Это тот же самый метод, которым рисуются кривые сглаживания в
большинстве финансовых мини-графиков.

`CORE_VERSION` обновлён до `6.1`.

### v6.2: полноэкранная "Таблица всех валют" — тап по валюте, ввод суммы, пересчёт всей таблицы

Запрошено: первый столбец — флаг+код валюты, второй — сумма; тап по строке
выбирает, в какую валюту вводится сумма, вся таблица пересчитывается сразу;
строки разделены полоской (как в приложениях-обменниках).

Важное ограничение WidgetKit, о котором стоит сказать сразу: сама плитка на
Home Screen — это статичный снапшот, который перерисовывает iOS, а не живое
приложение с клавиатурой. **Ввод суммы с клавиатуры и живой пересчёт таблицы
физически не могут жить прямо на плитке** — так работает вообще любой
iOS-виджет (не только этот). Поэтому эта функция реализована как отдельный
полноэкранный экран внутри Scriptable, который открывается из меню (тап по
плитке → меню → **«Таблица всех валют»**).

Как это выглядит и работает:

- Список валют (по умолчанию `USD, EUR, RUB, VND, THB, IDR`) — в каждой
  строке слева флаг+код (`🇺🇸 USD`), справа сумма. Строки разделены тонкой
  полоской
- Тап по любой строке делает её "активной" — именно в эту валюту теперь
  вводится сумма с клавиатуры внизу экрана; строка подсвечивается синим
- Все остальные строки пересчитываются мгновенно (прямо в WebView, без
  обращения к сети) по кросс-курсу через сохранённые курсы валют
- Список валют для этой таблицы настраивается отдельно от пар виджета:
  меню → **«Валюты для таблицы»**, формат `USD,EUR,RUB,VND,THB,IDR`
- Активная валюта и последняя введённая сумма запоминаются между открытиями
  таблицы

`CORE_VERSION` обновлён до `6.2`.

### v6.3: сама плитка на Home Screen теперь тоже показывает таблицу всех валют — и исправлен баг с клавиатурой, уходящей за экран

Два отдельных запроса:

1. **«На экран виджета вывести таблицу всех валют»** — раньше плитка
   показывала старый список пар `147 USD → 12 241,3 RUB` (со спарклайном и
   % изменения), а полноэкранная таблица из v6.2 была отдельной, самостоятельной
   функцией. Теперь это **одна и та же таблица** — сама плитка на Home Screen
   рисует ровно тот же список валют, ту же активную валюту и ту же сумму
   (`tableCurrencies` / `tableActiveCurrency` / `tableAmount`), что и
   полноэкранный экран. Старая система "пар и суммы для виджета"
   (`DEFAULT_PAIRS`, `widgetAmount`, «Настроить пары для виджета», «Задать
   сумму для виджета», «Конвертировать сумму») удалена целиком — она полностью
   заменена таблицей, и держать обе параллельно было бы просто путаницей.
2. **«Тап по виджету — переход в таблицу всех валют, с набором цифр»** — тап
   по плитке целиком (кроме значка обновления в углу) теперь **сразу**
   открывает ту же полноэкранную таблицу с клавиатурой, а не общее меню.
   Общее меню (настройка списка валют, принудительное обновление курсов)
   осталось доступно, но только если запустить сам скрипт вручную в
   приложении Scriptable (▶ Play) — там `queryParameters` пустые, и код
   понимает, что это не тап по плитке.

**Баг с клавиатурой, уходящей за экран** — классическая ловушка flexbox:
контейнер списка валют (`.list`) был растягивающимся flex-элементом с
`overflow-y: auto`, но без `min-height: 0`. По умолчанию flex-элемент не
сжимается меньше своего собственного контента, даже если стоит `overflow:
auto` — то есть список валют требовал себе столько высоты, сколько занимал
весь его контент, а не столько, сколько оставалось свободного места. Из-за
этого блок с клавиатурой внизу выталкивался за пределы `100vh`, а `overflow:
hidden` на `<body>` не давал прокрутить страницу до этого невидимого остатка
— именно так терялся нижний ряд цифр. Добавлено `min-height: 0` на `.list`
(плюс чуть уменьшены отступы у строк и кнопок клавиатуры, чтобы было больше
запаса) — теперь список валют сжимается и скроллится сам, а клавиатура из 4
рядов (все цифры 0-9, запятая, backspace) всегда целиком видна снизу.

`CORE_VERSION` обновлён до `6.3`.

### v6.4: центрирование, крупнее шрифт, ярче разделитель, без значка обновления

Четыре точечных правки по внешнему виду самой плитки:

- **Строки центрированы по вертикали.** Раньше блок строк был прижат к
  верхнему краю плитки, и если валют в списке было меньше, чем влезает по
  высоте (например 4 из возможных 5 на `medium`), под ними оставалось пустое
  место снизу. Теперь весь блок строк центрируется по вертикали внутри
  плитки — пустое место (если оно есть) делится поровну сверху и снизу.
- **Шрифт кода валюты и суммы заметно крупнее** на всех размерах виджета.
- **Разделительная полоска между валютами сделана ярче** — раньше она была
  почти незаметна на тёмном фоне.
- **Убран значок обновления** в правом нижнем углу — на плитке остаются
  только курсы, никаких служебных иконок. Обновить курсы вручную всё ещё
  можно через меню (▶ Play в приложении → «Обновить курсы сейчас»), а тап
  по самой плитке при этом продолжает сразу открывать полноэкранную таблицу.

`CORE_VERSION` обновлён до `6.4`.

### v6.5: отступы 15pt, крупнее суммы, флаг BRL, фикс обновления и клавиатуры

Пять точечных правок по скриншотам с телефона:

- **Отступ плитки сверху и снизу — 15pt** (раньше 8–10pt), строки больше не
  прилипают к скруглённым краям виджета.
- **Суммы крупнее кода валюты** на всех размерах плитки и в полноэкранной
  таблице (раньше кегль почти совпадал).
- **Флаг Бразилии 🇧🇷 для BRL** — раньше для реала показывался нейтральный
  плейсхолдер `💱`, потому что `BRL` не было в `CURRENCY_META`.
- **«Не успели обновить за 15с»** — провайдеры курсов теперь стартуют
  параллельно (побеждает первый валидный ответ), а не по очереди с
  `timeoutInterval=10`. Бюджет ручного «Обновить курсы сейчас» — 20с.
- **Сетка цифр в таблице поднята выше** — `flex: 1 1 0` у списка, компактнее
  кнопки и без лишнего `safe-area` снизу, чтобы нижний ряд (`, 0 ⌫`) не
  обрезался chrome'ом Scriptable.

`CORE_VERSION` обновлён до `6.5`.

### v6.6: без превью после Play + надёжное обновление курсов

- **Убрано всплывающее окно предпросмотра** после ▶ Play / тапа по виджету
  (`presentMedium()`). Раньше каждый запуск открывал модалку «Close» с
  плиткой; теперь скрипт только обновляет Home Screen через
  `Script.setWidget` и закрывается.
- **«Не успели обновить за 20с»**: в Scriptable параллельные `Request.loadJSON`
  иногда зависают навсегда (игнорируя `timeoutInterval`). Курсы снова
  запрашиваются **по очереди**, каждый URL жёстко ограничен `withTimeout(3с)`
  (макс. ~9с на все попытки). Добавлены `.min.json` зеркала fawaz. Если сеть
  не ответила — показывается понятное сообщение с возрастом кэша, а не общий
  таймаут.

`CORE_VERSION` обновлён до `6.6`.

### v6.7: курсы на мобильной сети + без синей подсветки на плитке

- **Таймаут провайдера 10с** (вместо 3с): на мобильной сети 3с не хватало на
  DNS+TLS, все URL отваливались подряд → «Сеть не ответила» при живом кэше.
- Первый URL — **jsdelivr** (тот же CDN, что доставляет core на телефон),
  плюс `open.er-api.com` и `api.exchangerate-api.com/v4`; ответ читается через
  `loadString` + `JSON.parse`.
- Если кэш ещё свежий, а повторный запрос не прошёл — сообщение
  **«Курсы актуальны»**, а не ошибка.
- **Убрана синяя подсветка** активной строки на Home Screen плитке (в
  полноэкранной таблице подсветка выбора осталась).

`CORE_VERSION` обновлён до `6.7`.

### v6.8: почасовые курсы вместо суточных

- Основной источник — **api.fxratesapi.com** (mid-market, обновление около
  раза в час), а не суточные снимки open.er-api / ECB-подобных API.
- Кэш на телефоне: **30 минут** (было 12 часов) — виджет чаще ходит в сеть.
- Подсказка WidgetKit: перерисовать примерно раз в **20 минут**.
- В меню возраст кэша показывает минуты и имя источника (`fxrates` / `fawaz` /
  `erapi`). Дневные API остаются фолбэком, если почасовой не отвечает.

`CORE_VERSION` обновлён до `6.8`.

### v6.9: мини-тренд и % за сутки на плитке

На каждой строке (кроме USD):

- **спарклайн** — сглаженная кривая курса к USD за ~7 дней;
- **▲/▼ %** — изменение за сутки относительно USD.

История копится локально и сразу подсеивается timeseries с fxratesapi,
чтобы график появился при первом же ▶ Play, а не через сутки ожидания.

`CORE_VERSION` обновлён до `6.9`.

### v6.10: крупные шрифты обратно, тренд по центру

- Шрифты кода/суммы и отступы строк — как в v6.8 (без сжатия).
- Padding плитки снова `15 / 16 / 15 / 16`.
- Спарклайн и ▲/▼ % стоят **по центру между** кодом валюты и суммой.

`CORE_VERSION` обновлён до `6.10`.

### v6.11: строки больше не вылезают за край

- Padding сверху/снизу **14pt**.
- Убраны внешние spacer'ы и жирные зазоры у разделителей.
- Свободная высота делится **поровну** между промежутками строк.
- Тренд и % в одну линию по центру (ниже строка → всё влезает).

`CORE_VERSION` обновлён до `6.11`.

### v6.12: тренд у USD + выравнивание графика и %

- Тренд и % считаются к **активной** валюте ввода (не только к USD) —
  поэтому у строки USD они появляются, когда активна, например, RUB.
- У активной строки (курс к себе = 1) тренда по-прежнему нет.
- График и ▲% в одной высоте по центру, зазор между ними увеличен.

`CORE_VERSION` обновлён до `6.12`.

### v6.13: USD всегда с трендом, график и % — одна картинка

- Для **USD** пара всегда есть: к активной валюте или к первой другой
  из списка (не USD→USD).
- Спарклайн и ▲% рисуются **одним** `DrawContext` — выравнивание по
  высоте гарантировано, зазор между ними увеличен.

`CORE_VERSION` обновлён до `6.13`.

### v6.14: проценты строго друг под другом

Фиксированные ширины колонок кода и тренда/% — ▲/▼ % больше не
смещаются из‑за разной длины текста или флага.

`CORE_VERSION` обновлён до `6.14`.

## Поведение (v6.14)

Референс-мокап лежит в `mockups/minimal.png` (исходник — `mockups/minimal.html`).
Это прямой HTML/CSS-рендер с теми же цветами и текстом, что и в
`createTableWidget()` внутри `CurrencyConverterCore.js` — то же самое, что
показывает и полноэкранная таблица (v6.2), только без клавиатуры внизу.

<img src="mockups/minimal.png" alt="Плитка виджета — таблица всех валют" width="480" />

- Сплошной тёмный фон `#0B0C0E`
- Один и тот же список валют, что и в полноэкранной таблице:
  **флаг+код** слева, **сумма** справа, строки разделены заметной полоской
- Активная валюта (та, в которую введена сумма) подсвечена синим —
  ровно как в полноэкранной таблице
- Крупный шрифт, блок строк центрирован по вертикали внутри плитки
- Никакой служебной метки версии/времени и никакого значка обновления на
  самой плитке — только курсы
- На каждый размер видно столько строк, сколько влезает: `small` — 3,
  `medium` — 5, `large` — 7 (или меньше, если валют в списке не хватает)

### Тап по плитке

- Тап по плитке целиком, в любом её месте (на любом размере) сразу открывает
  полноэкранную таблицу всех валют с клавиатурой — см. «v6.2» и «v6.3» выше.
  Отдельной тап-зоны у значка обновления больше нет (см. «v6.4») — весь тап
  по плитке ведёт в одно и то же место

### Экран блокировки (Lock Screen, iOS 16+)

- Добавь виджет Scriptable прямо на экран блокировки — поддерживаются все три
  формата: `accessoryRectangular` (2 строки), `accessoryCircular` (код валюты
  + значение) и `accessoryInline` (одна строка в верхней части). Показывается
  активная валюта, конвертированная в первую из списка, что отличается от
  неё. Цвет там всегда монохромный — так решает сама iOS

### Общее для всех размеров

- Запуск скрипта вручную в приложении Scriptable (▶ Play, не тап по плитке)
  открывает меню: «Открыть таблицу всех валют», «Валюты для таблицы»,
  «Обновить курсы сейчас». В заголовке меню — текущий `CORE_VERSION`, для
  диагностики того, подтянулось ли обновление кода
- «Валюты для таблицы» — список валют вида `USD,EUR,RUB,VND,THB,IDR`,
  используется и на плитке, и в полноэкранной таблице
- Курсы кэшируются на устройстве — при отсутствии сети показывается последний
  известный курс с пометкой давности
- Разные виджеты на Home Screen можно настроить на разные списки валют через
  **Edit Widget → Parameter**, формат: `USD,EUR,RUB,VND,THB,IDR` (список
  кодов через запятую, без пар)

## Источники курсов (бесплатно, без ключа)

1. `api.fxratesapi.com` — **основной, ~раз в час** (mid-market)
2. `@fawazahmed0/currency-api` (jsdelivr CDN) — фолбэк, обычно раз в сутки
3. `open.er-api.com` — фолбэк, обновление раз в сутки

Кэш на устройстве считается свежим **30 минут**. Курс между любыми двумя
валютами считается кросс-курсом через `USD` одним запросом.

## Файлы

| Файл | Назначение |
|------|------------|
| `CurrencyConverter.js` | короткий bootstrap для Scriptable (вставить один раз) |
| `CurrencyConverterCore.js` | логика: курсы, кэш, конвертация, отрисовка виджета |

## Установка на iPhone

1. Установи бесплатное приложение **Scriptable** из App Store
2. Создай новый скрипт, назови `CurrencyConverter`
3. Вставь ВЕСЬ код из `CurrencyConverter.js` → ▶ Play
   (он сам скачает `CurrencyConverterCore.js` с GitHub)
4. Добавь виджет Scriptable на Home Screen, выбери скрипт `CurrencyConverter`
   (для экрана блокировки — долгий тап по Lock Screen → Customize → добавь
   виджет Scriptable в нужном формате и тоже выбери `CurrencyConverter`)
5. Чтобы GitHub-загрузка работала — репозиторий `projects_po` должен быть
   Public (Settings репозитория → Change visibility)

## Если тап по виджету не открывает таблицу

**Проверь это в первую очередь** — это самая частая причина, и код тут ни при
чём: долгий тап по виджету на Home Screen → **Edit Widget** → пункт
**«When Interacting» (При взаимодействии)** → должно быть выставлено
**«Run Script» (Запустить скрипт)**. Если там стоит что-то другое (или
значение не выбрано) — тап просто открывает приложение Scriptable, не
запуская сам скрипт, и таблица в принципе не может появиться, независимо от
того, что в коде.

Второй вариант причины — сеть: раньше каждое открытие приложения по тапу пыталось
перекачать код с GitHub без ограничения по времени, и на медленном/нестабильном
соединении таблица могла не успевать появиться. Теперь:

- обновление кода при тапе ограничено 7 секундами — если сеть не успела,
  просто используется уже скачанная версия и таблица открывается сразу
- любая непредвиденная ошибка в скрипте показывается алертом с текстом
  ошибки вместо тихого сбоя

Если после обновления (`▶ Play` в приложении) таблица всё ещё не появляется —
скорее всего, это сама иконка на Home Screen ещё не подхватила новый скрипт:
удали и заново добавь виджет, либо один раз открой скрипт в приложении и
нажми ▶ Play вручную (это откроет общее меню, а не таблицу — тап по плитке и
ручной ▶ Play ведут в разные места, см. «Поведение (v6.4)» выше).

## Обновить логику

Правишь `CurrencyConverterCore.js` в этом репозитории → пушишь → **на
телефоне открываешь скрипт `CurrencyConverter` в самом приложении Scriptable
и жмёшь ▶ Play (не через тап по виджету на Home Screen!)** — подтянется
свежая версия. Сразу проверь номер версии в заголовке открывшегося меню
(«Конвертер валют · vX.X») — если он совпадает с `CORE_VERSION` в
`CurrencyConverterCore.js`, обновление точно доехало. Виджет на Home Screen
подхватит новую логику при следующей системной перерисовке (или обнови
сам — удали и заново добавь виджет).

Проверить, что именно сейчас реально отдаёт GitHub (без телефона), можно
одной командой:

```bash
curl -s "https://raw.githubusercontent.com/assssdrew/projects_po/cursor/currency-converter-scriptable-widget-bfeb/projects/currency-converter-widget/CurrencyConverterCore.js" | grep 'CORE_VERSION ='
```

Если она печатает не тот номер версии, что в этом README — значит, GitHub
ещё не отдаёт свежий пуш (редко и обычно занимает не больше пары минут), и
дело не в телефоне и не в Scriptable.
